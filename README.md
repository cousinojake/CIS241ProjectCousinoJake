# CIS241ProjectCousinoJake
Running macOS High Sierra version 10.13.5

imput files into the directory sample_files1 then run the program using ./Hash

if you need to recompile, use the command "gcc -o Hash Hash.c"
